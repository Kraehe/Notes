<%#= form_for @note, url: notepad_path(@note) do |f| %>
<%= form_for @note, :url => new_notepad_path(@note) do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :text, class: "form-control" %><br>
  </div>
  <div class="form-group">
  <%= f.label :url %>
  <%= f.text_field :url %><br>
</div>
<div class="form-group">
  <%= f.label :imageurl %>
  <%= f.text_field :imageurl %><br>
</div>
  <%= f.submit class:"btn btn-danger" %>
<% end %>
